// StackTable  -  placeholder. Step rendering is driven by the legacy render()
// pipeline in src/app/utils/legacy.js, which writes the matching markup into
// #content. This component file is kept to preserve the requested file
// structure and to host React-native versions in a future refactor.
export default function StackTable() {
  return null;
}

